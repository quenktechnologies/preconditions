{
  "name": "@quenk/preconditions",
  "version": "2.3.2",
  "description": "An async friendly version of a Criteria pattern.",
  "main": "lib/Sync.js",
  "types": "lib/Sync.d.ts",
  "repository": "https://github.com/quenktechnologies/preconditions",
  "scripts": {
    "test": "./node_modules/.bin/mocha --opts mocha.opts test",
    "build": "rm -R lib; mkdir -p lib; ./node_modules/.bin/tsc --project src",
    "docs": "./node_modules/.bin/typedoc --ignoreCompilerErrors --out docs --tsconfig src/tsconfig.json && echo 'DO NOT DELETE!' > docs/.nojekyll"
  },
  "keywords": [
    "criteria"
  ],
  "author": "Lasana Murray <dev@quenk.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/bluebird": "^3.5.8",
    "@types/mocha": "^2.2.42",
    "@types/node": "^8.0.25",
    "mocha": "^2.3.4",
    "must": "^0.13.1",
    "ts-node": "^3.3.0",
    "typedoc": "^0.8.0",
    "typescript": "^2.4.2"
  },
  "engines": {
    "node": "^4.0.0"
  },
  "dependencies": {
    "@quenk/polate": "^1.2.0",
    "afpl": "^0.5.4",
    "bluebird": "^3.5.0"
  }
}
