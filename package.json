{
    "name": "criterias",
    "version": "0.0.1",
    "description": "Transform an object by applying some criteria.",
    "main": "index.js",
    "repository": "https://github.com/metasansana/pipes.git",
    "scripts": {
        "test": "./node_modules/.bin/mocha --opts mocha.opts test",
        "build": "mkdir -p lib; ./node_modules/.bin/babel -D src -s inline -d lib"
    },
    "keywords": [
        "transform",
        "criteria"
    ],
    "author": "Lasana Murray <lmurray@trinistorm.org> (http://trinistorm.or)",
    "license": "Apache-2.0",
    "devDependencies": {
        "babel": "^5.8.34",
        "mocha": "^2.3.4",
        "must": "^0.13.1"
    },
    "engines": {
        "node": "^4.0.0"
    }
}
